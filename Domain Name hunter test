# string to search in file
word = 'mirati'
with open(r"domain-names.txt", 'r') as fp:
    # read all lines in a list
    lines = fp.readlines()
    for line in lines:
        # check if string present on a current line
        if line.find(word) != -1:
            print('New Domain Registered')
            print(line)
