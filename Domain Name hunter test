with open(r"domain-names.txt", 'r') as f:
    for index, line in enumerate(f):

        # search string
        if 'mirati' in line:
            print('String found in a file')
            # don't look for next lines
            break
        else:
            print('String not found in file')
            break
